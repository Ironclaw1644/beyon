# dependencies / build output
node_modules
.next
out
*.tsbuildinfo

# deployment
.vercel

# environment (real values never committed; names live in .env.example)
.env*
!.env.example

# raw source photos, logo originals, and reference PDFs
assets/

# local tooling
.claude/
.DS_Store
*.log
